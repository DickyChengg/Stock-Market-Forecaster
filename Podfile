# Uncomment the next line to define a global platform for your project
platform :ios, '13.2'

target 'StockMarketForecaster' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Charts', '3.5.0'
  pod 'SwiftCSV', '0.5.6'
  pod 'SwiftDate', '6.1.0'
  pod 'SwiftyJSON', '5.0'

end
